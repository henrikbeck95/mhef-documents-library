package com.mhef.library.documents.dao.content;

/*
import org.apache.poi.POIXMLDocument;
// readDocxFile(pathFileInputDocument);
//		String fileName = "/assets/data/01861307.pdf";
public class ContentReadDocx {
    public static void readDocxFile(String filePath) {
        try {
            XWPFDocument doc = new XWPFDocument(POIXMLDocument.openPackage(filePath));

            // Iterate through paragraphs in the docx file
            for (XWPFParagraph paragraph : doc.getParagraphs()) {
                // Process the paragraph text here
                String paragraphText = paragraph.getText();

                // Extract the tags or markdown present in the paragraph text

                // Example: Print the paragraph text
                System.out.println(paragraphText);
            }

            doc.close();
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}
 */